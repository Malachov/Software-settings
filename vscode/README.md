# VS Code plugins, settings JSON, keyboard shortcuts JSON

My own json settings. Mostly for python optimized.

Contains used plugins, global settings and key bindings should be synced by VS Code, but for sure it's here.

## Tips and How tos

Pylance import warning - set in workspace settings json

"python.analysis.extraPaths": ["NAME_OF_LIBRARY"],

## Some of used plugins

You can install plugins with 

`bash vscode/install-extensions.sh`